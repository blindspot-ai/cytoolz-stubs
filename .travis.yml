language: python
matrix:
  include:
    - python: 3.7
      dist: xenial
      sudo: true
    - python: 3.6

notifications:
  email: false

install:
  - make setup

script:
  - make release-check

cache:
  directories:
  - "$HOME/.cache/pip"
